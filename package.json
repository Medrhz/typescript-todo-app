{
  "name": "typescript-todo-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && live-server --mount=/dist:./dist",
    "watch": "npx tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}